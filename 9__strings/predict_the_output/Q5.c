#include <stdio.h>

int main() {
    char str[] = {"Hello How are you"};  // Correct string initialization with char array
    // printf("%s\n", str);               // Print the string using %s
    puts(str);
    return 0;
}
