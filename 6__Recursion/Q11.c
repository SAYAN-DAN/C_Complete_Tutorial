#include<stdio.h>
int maze(int cr,int cc,int er,int ec) {
    int totalways = rightways +downways;
}

int main() {
    int a, b;
    printf("Enter number of rows of the maze: ");
    scanf("%d", &a);
    printf("Enter number of coloum of the maze: ");
    scanf("%d", &b);
    int numofways = maze(1,1,n,m);
    printf("%d", numofways);
    return 0;
}

